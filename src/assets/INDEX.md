# assets/ — page CSS and JS

| File | What |
| :-- | :-- |
| `cc.css` | Styles, all scoped under `.cc-root`. |
| `cc-model.js` | Snapshot → session model and formatting. No DOM. |
| `cc-view.js` | Polls `state.php`, renders the page, keeps each viewer's toggles. |
